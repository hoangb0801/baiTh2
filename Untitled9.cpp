#include<bits/stdc++.h>
#include<string.h>
using namespace std;

int main(){
    string s,a;
    getline(cin,s);
    getline(cin,a);
    while(s.find(s1)!=string::npos){
		s.erase(s.find(s1), s1.length());
	}
	cout<<s;
	return 0;
}
	

